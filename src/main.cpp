#include <iostream>

int Sum(int a, int b);
int difference(int k, int l); 
int multiplication(int h, int j);

int main(){
    std::cout << "\n=== Calculation Results ===" << std::endl;
    std::cout << "Sum: " << Sum(a, b) << std::endl;
    std::cout << "Difference: " << difference(k, l) << std::endl;
    std::cout << "Multiplication: " << multiplication(h, j) << std::endl;

    return 0;
}

